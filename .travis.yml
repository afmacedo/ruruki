language: python
python:
  - "2.7"
install:
  - pip install tox-travis
script: tox
# whitelist branches to build
# branches:
#   - master
notifications:
  email:
    - jenmud@gmail.com
    on_success: always
    on_failure: always
  
